# Trial 0012 - Three-Agent Conflict Dogfood

Date: 2026-06-01
Source state: `87b7f3b` plus MVP 0.40 working tree
Target repo: `/var/folders/zv/p_qkzppj07n4t10f5c19gp480000gn/T/tmp.AN647oeoOC`
Agent tool: `codex exec v0.135.0`

## Goal

Validate whether Anvics can support a realistic conflict workflow where three agents edit the same function lines for different task intents, then a resolver step combines the candidate reviews into one accepted patch.

This is not a test of native automatic conflict resolution. The current product only promises overlap detection, isolated workspaces, review notes, and operator-mediated resolution.

## Setup

The target repo was generated by `scripts/three_agent_conflict_trial_prepare.sh`.

Initial function:

```rust
pub fn describe_score(score: i32) -> String {
    if score >= 80 {
        "strong".to_owned()
    } else {
        "needs work".to_owned()
    }
}
```

Agents:

- Agent A: change thresholds so `>= 90` is excellent, `70..=89` is passing, lower scores need work.
- Agent B: include the numeric score in every non-invalid returned label.
- Agent C: make negative scores return `invalid score`.

## Candidate Runs

All three agents:

- Read the generated packet.
- Read the Anvics skill.
- Ran `agent enter` before editing.
- Edited only their workspace.
- Used `workspace diff`.
- Created an `agent checkpoint`.
- Ran `coordination status`.
- Verified with `cargo test`.

Candidate reviews:

- Agent A review: `224b572f-77ec-418b-a03c-5b86a405f15f`.
- Agent B review: `c0af8668-02aa-498b-aabd-11817d24d77c`.
- Agent C review: `15202ed7-a552-4667-9991-06f3558c1052`.

Each candidate review reported overlap notes for the other two threads on `src/lib.rs`.

## Resolver Run

Resolver thread: `f20e7913-116a-41f8-8c30-9b5d17d45995`
Resolver workspace: `db3316ba-6d5e-49de-ad97-69001ac0ac69`

The first resolver launch failed before editing because the generated packet used bare `anvics`, but `anvics` was not installed on `PATH` in this trial environment. The resolver correctly stopped instead of guessing with Git.

The resolver was relaunched with the repo-local Cargo command prefix:

```sh
cargo run -q -p anvics-cli --bin anvics --manifest-path /Users/simon/Repos/anvics/Cargo.toml --
```

The resolver then:

- Read all three candidate review markdown files.
- Entered the workspace.
- Combined the three intents into one implementation.
- Used `workspace diff --format patch`.
- Created checkpoint `e419de7f-70da-468c-ad38-812d16878b93`.
- Ran `coordination status`, which reported overlap on `src/lib.rs` with all three candidate workspaces.
- Verified with `cargo test`, exit code 0.

Final accepted review: `b51b93c6-f2c1-4b85-95b5-3b6978f1a9a6`.
Publication: `c9eeb396-4a67-4d2f-9ade-2508d924a59e`.
Patch: `/var/folders/zv/p_qkzppj07n4t10f5c19gp480000gn/T/tmp.AN647oeoOC/resolved.patch`.

## Final Behavior

```rust
pub fn describe_score(score: i32) -> String {
    if score < 0 {
        "invalid score".to_owned()
    } else if score >= 90 {
        format!("score {score}: excellent")
    } else if score >= 70 {
        format!("score {score}: passing")
    } else {
        format!("score {score}: needs work")
    }
}
```

The exported patch applied cleanly to a clean copy and `cargo test` passed with 4 tests.

## Findings

- Anvics handled the mechanical part well: isolated workspaces, known changed paths, candidate reviews, checkpointing, and final publication all worked.
- The overlap signal was useful and clear once each agent checkpointed or finished. Before that, related work appeared as unknown possible changes.
- Review markdown was enough for a resolver to understand the three candidate intents without reading full transcripts.
- The resolver workflow is still manually assembled. The operator had to create a resolver task and paste review paths into it.
- Command-prefix hygiene remains important. The resolver packet used bare `anvics` because it was prepared without `ANVICS_AGENT_COMMAND`; the agent correctly stopped, but this is friction.
- Coordination status compares changed paths, not semantic intent. It did not know the resolver was intentionally resolving candidates until the resolver changed `src/lib.rs`.

## Recommendation

This validates the current product as an operator-mediated conflict workflow, not a native conflict-resolution system.

Next meaningful product slice: add a first-class `agent resolve` or `conflict prepare` command that takes multiple review ids, creates a resolver thread/workspace, embeds the candidate review paths and command prefix correctly, and records the source candidate reviews on the resolver review. This should prepare a resolver task, not spawn or require a new agent runtime.

## Follow-Up: MVP 0.41

MVP 0.41 adds `anvics agent resolve --review ...` for this exact workflow. The command prepares a resolver thread/workspace from candidate reviews that share one base snapshot, writes a resolver packet with candidate review markdown paths and compact evidence/overlap context, supports an explicit `--agent-command` prefix, and carries `source_review_ids` into the final resolver review markdown. The operator decides whether the current agent, a human, or an operator-selected external agent handles that resolver packet.
